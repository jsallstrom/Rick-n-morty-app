import styled from "styled-components";

export const EpisodeList = styled.div`
     display: flex;
     flex-direction: row;
     flex-wrap: wrap; /*This one makes the elements multi line not single line rows */
     justify-content: center;
     margin-top: 15px;
`;
